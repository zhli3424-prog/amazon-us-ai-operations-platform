[pytest]
testpaths = tests
norecursedirs = data backups outputs node_modules .venv .test-* .audit-* pytest-tmp
markers =
    browser: real browser end-to-end checks
    performance: bounded local performance checks
