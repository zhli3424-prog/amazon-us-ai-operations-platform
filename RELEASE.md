# 发布与回滚清单

当前应用版本：`0.3.0`，数据库版本：`21`。本清单面向单店铺、单机 Web + Worker 部署；真实 Amazon 渠道上线仍须单独完成 Sandbox 和生产验收。

## 发布前

- 固定镜像版本，不使用 `latest`；保存当前应用镜像标签与数据库版本。
- 设置所有 `.env.example` 中标为生产必需的独立密钥、附件扫描、异地备份和审计检查点路径。
- 运行 `py -3.12 -m pytest`、`py -3.12 scripts/check_migrations.py` 与 `py -3.12 -m pip_audit -r requirements.txt`。
- 运行一次双份备份，确认 `backup_runs.status=success`，并从副本恢复到新文件完成演练。
- 记录上线负责人、审批人、窗口、回滚镜像、备份路径和业务验收人。

## 发布

1. 暂停写流量和 Worker，确认没有 `running` 状态的任务。
2. 执行 `docker compose -p cross-border-ai run --rm migrate`；迁移任务必须独占数据库。
3. 启动 Web 和 Worker，检查 `/health/live`、带令牌的 `/health/ready`、Worker 心跳与数据库版本。
4. 验收登录、订单同步、库存同步、失败处置、文案审批、模拟发布、客服动作和审计检查点。
5. 观察错误日志、Webhook 失败、备份失败与同步失败至少一个完整维护周期。

## 回滚

- 仅应用回滚：迁移向后兼容时，停止 Web/Worker，切回原镜像后重新检查就绪状态。
- 数据回滚：先停止 Web/Worker，保留故障库取证；用发布前备份恢复到一个新数据库文件，核对 SHA-256、`PRAGMA integrity_check` 和版本后，再原子切换 `DATABASE_PATH`。恢复脚本拒绝覆盖已有文件。
- 不执行 SQL 降级脚本，不直接覆盖故障库；涉及发布后真实业务数据时，先由业务负责人决定补录或回放方案。

## 恢复目标

- 运维目标为 RPO 不超过 24 小时、RTO 不超过 60 分钟；这是需要用定期恢复演练验证的目标，不是代码层保证。
- 每季度至少完成一次“副本恢复到新路径 → 启动只读验收 → 记录实际用时”的演练；未达标则提高备份频率或升级数据库架构。
